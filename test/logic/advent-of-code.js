const {expect} = chai;
import ts from "tacitscript";

export default () => {
	xdescribe("", () => {
		/*ts

		*/
		it("", () => expect().eql());
	});

	describe("Advent of Code", () => {
		describe("", () => {
			/*ts

			*/
			it("", () => expect().eql());
		});
	});
};